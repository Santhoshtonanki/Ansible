- name: variables bigip_profile_persistence_src_addr:
  hosts: localhost
  connection: local
  vars: 
  - name: preference from play level
  vars_files:
  - course.yaml
  vars_prompt: 
  - name: "username"
    prompt: "what is your username?"
    private: "no"
  tasks:
  - name: 
    vars: 
      name: preference from task level
    ansible.builtin.debug:
      msg: "name is {{ username }}"
