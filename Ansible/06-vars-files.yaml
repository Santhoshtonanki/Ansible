- name: play level variables
  hosts: localhost
  connection: local
  vars_files:
    - course.yaml
  vars:
    Person_name: "santhosh tonanki"
    age: 29
    course: "DevOps with AWS"
    duration: "5 hours"
    trainer: "Sivakumar Reddy"
  tasks:
    - name: Print the course information
      ansible.builtin.debug:
        msg: "Hello world!  Person_name is {{ Person_name }}, age is {{ age }}, course is {{ course }}, Duration is {{ duration }}, and finally trainer is {{ trainer }}"
        