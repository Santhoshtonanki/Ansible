- name: loops
  hosts: localhost
  connection: local
  tasks:
  - name: install packages
    ansible.builtin.debug:
      name: "{{ item }}"
      state: latest
    loop:
    - nginx
    - mysql
    - zip
